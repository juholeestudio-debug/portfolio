---
interface Props {
  home?: boolean;
  mobileFixed?: boolean;
}

const {
  home = false,
  mobileFixed = false,
} = Astro.props;
---

<header
  class:list={[
    "site-header",
    {
      home,
      "mobile-fixed": mobileFixed,
    },
  ]}
>
  <a class="site-name" href="/">Juho Lee</a>

  <nav>
    <a href="/work">Work</a>
    <a href="/cv">CV</a>
    <a href="/contact">Contact</a>

    <a class="production-link" href="/production">
      Production
      <span>Internal</span>
    </a>
  </nav>
</header>

<style>
  .site-header {
    position: fixed;
    top: 28px;
    left: 32px;
    z-index: 1000;

    width: auto;
    height: auto;
    margin: 0;

    display: block;

    color: #111;
    font-family: Arial, Helvetica, sans-serif;
  }

  .site-name {
    display: block;
    margin: 0 0 20px;

    color: inherit;
    font-size: 20px;
    font-weight: 700;
    line-height: 1;
    letter-spacing: -0.02em;
    text-decoration: none;
  }

  nav {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 10px;
  }

  nav a {
    display: block;

    color: inherit;
    font-size: 16px;
    font-weight: 400;
    line-height: 1.25;
    text-decoration: none;
  }

  nav a:hover {
    text-decoration: underline;
    text-underline-offset: 4px;
  }

  .production-link {
    margin-top: 14px;
    opacity: 0.75;
    transition: opacity 0.2s ease;
  }

  .production-link:hover {
    opacity: 1;
  }

  .production-link span {
    display: inline-block;
    margin-left: 6px;

    padding: 2px 5px;

    border: 1px solid #111;

    font-size: 8px;
    font-weight: 700;
    line-height: 1;

    letter-spacing: 0.05em;
    text-transform: uppercase;

    vertical-align: middle;
  }

  @media (max-width: 760px) {
    .site-header {
      position: static;
      top: auto;
      left: auto;

      padding: 20px 18px 0;
    }

    .site-header.home,
    .site-header.mobile-fixed {
      position: fixed;
      top: 20px;
      left: 18px;
      padding: 0;
    }

    .site-name {
      margin-bottom: 10px;
      font-size: 20px;
    }

    nav {
      flex-direction: row;
      align-items: flex-start;
      gap: 15px;
      flex-wrap: wrap;
    }

    nav a {
      font-size: 14px;
    }

    .production-link {
      margin-top: 0;
    }

    .production-link span {
      display: none;
    }
  }
</style>